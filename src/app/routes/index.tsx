import { Routes } from "react-router-dom";

export const PlansRoutes = () => {
  return (
    <Routes>
      {/* <Route index element={<PlansLayout />} />
      <Route path="*" element={<NotFound />} /> */}
    </Routes>
  );
};
